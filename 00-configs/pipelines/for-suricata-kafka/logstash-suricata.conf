input {
  # May change based on how Suricata send log to LogStash
  beats {
    port => 5044
  }  
}

filter {
  mutate {
    add_field => {
      "job" => "suricata"
    }
  }
}

output {
  #stdout { codec => rubydebug }

  kafka {
    bootstrap_servers => "kafka:29092"
    codec => json
    topic_id => "suricata"
  }
}
